const config = {
  baseUrl: import.meta.env.PRODUCTION_HOST || 'https://api-ecommerce-dangcongduc.onrender.com',
  // baseUrl: 'http://localhost:4000',
  maxSizeUploadAvatar: 1048576 // bytes
}

export default config
